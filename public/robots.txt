# See https://www.robotstxt.org/robotstxt.html for documentation on how to use the robots.txt file

User-agent: *
Allow: /

# The API answers to clients rather than to crawlers, and the OpenAPI documents
# behind /api-docs are the reference pages in machine readable form.
Disallow: /api/
Disallow: /api-docs/

# Search results are variations of the breed list. They are crawlable so the
# "noindex, follow" on them can be read and the breeds they link to followed.

Sitemap: https://dogapi.dog/sitemap.xml
