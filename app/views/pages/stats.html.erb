<% page_heading "Stats" %>

<h3>Visits per hour</h3>
<%= line_chart Ahoy::Visit.group_by_hour(:started_at).count %>

<h3>Requests per hour V2</h3>
<%= line_chart Ahoy::Event.where_event('get_facts_v2').group_by_hour(:time).count %>

<h3>Requests per hour V1</h3>
<%= line_chart Ahoy::Event.where_event('get_facts_v1').group_by_hour(:time).count %>
