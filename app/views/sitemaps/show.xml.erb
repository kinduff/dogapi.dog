<?xml version="1.0" encoding="UTF-8"?>
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">
<% @urls.each do |url| %>
  <url>
    <loc><%= "#{request.base_url}#{url[:loc]}" %></loc>
    <% if url[:lastmod] %><lastmod><%= url[:lastmod] %></lastmod><% end %>
  </url>
<% end %>
</urlset>
