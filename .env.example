# Copy to .env and fill in. Never commit the real values.
#
# Umami analytics. Tracking is disabled unless UMAMI_USERNAME and
# UMAMI_PASSWORD are both set (see config/initializers/umami.rb).
UMAMI_URI_BASE=
UMAMI_USERNAME=
UMAMI_PASSWORD=
UMAMI_WEBSITE_ID=
